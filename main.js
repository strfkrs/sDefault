// This will return an ArrayBuffer with `addTwo.wasm` binary contents

const core = require("core_api");
self.log = require("logger");

self.log("reset");
core.init();

module.exports.loop = function () {
    
      core._set(c++);
      console.log(core._get(c), Game.cpu.getUsed());
}